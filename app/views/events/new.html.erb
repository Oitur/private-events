<%= form_for(@event) do |f| %>
  <%= f.label 'Event title', :title %>
  <%= f.text_field :title %>
  <%= f.label 'Date', :date %>
  <%= f.datetime_select :date %>

  <%= f.submit 'Create Event' %>
<% end %>