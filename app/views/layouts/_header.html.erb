<%= link_to 'Home', root_url %>
<% if session[:id] %>
  <h1>Logged in as: <%= link_to User.find(session[:id]).name, User.find(session[:id]) %></h1>
  <%= link_to 'Logout', logout_path, method: :delete %>
<% else %>
  <%= link_to 'Login', login_path %>
  <%= link_to 'Sign Up', new_user_path %>
<% end %>